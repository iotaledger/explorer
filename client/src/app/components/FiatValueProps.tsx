export interface FiatValueProps {
    /**
     * The value (in IOTAs) to convert to fiat.
     */
    value: number;
    /**
     * The optional additional classes.
     */
    classNames?: string;
}
