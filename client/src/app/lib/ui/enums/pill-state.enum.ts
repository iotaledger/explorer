export enum PillState {
    /**
     * The status is pending.
     */
    Pending = "pending",

    /**
     * The status is confirmed.
     */
    Confirmed = "confirmed",

    /**
     * The status is rejected.
     */
    Rejected = "rejected",
}
