export enum SlotState {
    /**
     * The slot is pending.
     */
    Pending = "pending",

    /**
     * The slot is committed.
     */
    Committed = "committed",

    /**
     * The slot is finalized.
     */
    Finalized = "finalized",
}
