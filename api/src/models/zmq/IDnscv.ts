/**
 * Neighbor DNS validations.
 */
export interface IDnscv {
    /**
     * Neighbor's hostname
     */
    neighborsHostname: string;

    /**
     * Neighbor's IP address
     */
    neighborsIPAddress: string;
}
