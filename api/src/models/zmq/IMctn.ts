/**
 * Number of transactions traversed during tip selection.
 */
export interface IMctn {
    /**
     * Total number of transactions that were traversed during tip selection.
     */
    totalTransactions: number;
}
