/**
 * Information about non-tethered neighbors that were added (available only on the testnet network).
 */
export interface IAntn {
    /**
     * URL of a non-tethered neighbor.
     */
    url: string;
}
